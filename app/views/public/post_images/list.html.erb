<h2>自身の投稿一覧</h2>
<% @post_images.each do |post_image| %>
<p>タイトル</p>
<%= post_image.title %>
<%= image_tag post_image.get_image(200,200) %>
<%= post_image.introduction %>
<p><%= link_to "#{post_image.messages.count} コメント", post_image_path(post_image.id) %></p>
<% end %>