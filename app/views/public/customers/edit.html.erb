<h2>ユーザー情報編集</h2>
<%= form_with model: @customer,url: customers_information_path, method: :patch do |f| %>

<%= f.label "ユーザー名" %>
<%= f.text_field :name %>
<%= f.label "メールアドレス" %>
<%= f.text_field :email %>

<%= f.submit "変更を保存" %>

<% end %>