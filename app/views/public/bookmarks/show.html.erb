<%= image_tag @bookmark.get_image %>
タイトル：<%= @post.title %>
説明：<%= @post.introduction %>
<div>
  <p>コメント件数：<%= @bookmark.comments.count %></p>
  <% @bookmark.comments.each do |comment| %>
    <p><%= image_tag bookmark.customer.get_image(100,100) %></p>
    <%= comment.customer.name %>
    <%= pcomment.created_at.strftime('%Y/%m/%d') %><%= comment.comment %>
  <% end %>
</div>
<div>
  <%= form_with model: [@bookmark, @bookmark] do |f| %>
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
  <% end %>
</div>